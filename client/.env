# 실제 로그인 API가 준비되면 아래 값을 채우고 .env로 복사하세요.
VITE_API_BASE_URL=http://localhost:4000/api

# true: authApi가 목(mock) 로그인을 사용합니다 (백엔드 없이 개발 가능).
# false: VITE_API_BASE_URL/auth/login 으로 실제 요청을 보냅니다.
VITE_USE_MOCK_API=false
